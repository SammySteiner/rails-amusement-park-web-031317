Attraction Show Page
<%= @attraction.name %>
<% if @user.admin %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% else %>
  <%= form_tag '/rides/new' do%>
    <%= hidden_field_tag 'ride_id', @attraction.id %>
    <%= button_tag "Go on this ride" %>
  <% end %>
<% end %>
